# Mini-SOC dependencies
# Core web dashboard
Flask==3.1.3

# Optional: enables live IP reputation lookups against AbuseIPDB when
# ABUSEIPDB_API_KEY is set. The app works fully offline without it.
requests==2.34.2

# Optional: enables PDF export of incident reports. The app falls back to
# Markdown/HTML-only reports if this isn't installed.
fpdf2==2.8.8
